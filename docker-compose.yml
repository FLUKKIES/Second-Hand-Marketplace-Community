services:
    postgres:
        image: pgvector/pgvector:pg16
        container_name: marketplace-db
        restart: always
        env_file:
            - .env
        environment:
            POSTGRES_USER: ${POSTGRES_USER}
            POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
            POSTGRES_DB: ${POSTGRES_DB}
        ports:
            - "${POSTGRES_PORT}:5432"
        volumes:
            - pgdata:/var/lib/postgresql/data
        networks:
            - marketplace-network

    pgadmin:
        image: dpage/pgadmin4
        container_name: marketplace-pgadmin
        env_file:
            - .env
        environment:
            PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
            PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD}
        ports:
            - "${PGADMIN_PORT}:80"
        depends_on:
            - postgres
        volumes:
            - pgadmin_data:/var/lib/pgadmin
        networks:
            - marketplace-network

    ollama:
        image: ollama/ollama:latest
        container_name: marketplace_ollama_ai
        entrypoint: /bin/sh
        command: -c "ollama serve & sleep 5 && ollama pull bge-m3 && wait"
        volumes:
            - ollama_storage:/root/.ollama
        ports:
            - "11434:11434"
        networks:
            - marketplace-network
    # backend:
    #     build: ./backend
    #     container_name: marketplace-backend
    #     env_file:
    #         - ./backend/.env
    #     ports:
    #         - "${PORT}:${PORT}"
    #     volumes:
    #         - ./backend:/app
    #         - /app/node_modules
    #     # environment:
    #         # *** จุดสำคัญ: ประกอบ URL สำหรับใช้ "ภายใน" Docker (ใช้ชื่อ host: postgres) ***
    #         # DATABASE_URL: "postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?schema=public"
    #         # PORT: ${PORT}
    #     depends_on:
    #         - postgres
    #     networks:
    #         - marketplace-network

    # # Frontend
    # frontend:
    #     build: ./frontend
    #     container_name: marketplace-frontend
    #     env_file:
    #         - .env
    #     ports:
    #         - "${FRONTEND_PORT}:3000"
    #     volumes:
    #         - ./frontend:/app
    #         - /app/node_modules
    #     environment:
    #         NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
    #     depends_on:
    #         - backend
    #     networks:
    #         - marketplace-network

    # nginx:
    #     image: nginx:alpine
    #     container_name: marketplace-nginx
    #     volumes:
    #         - ./nginx.conf:/etc/nginx/nginx.conf:ro
    #     ports:
    #         - "8080:80" # เผื่ออยากเทสผ่าน localhost:8080
    #     extra_hosts:
    #         - "host.docker.internal:host-gateway"
    #     networks:
    #         - marketplace-network

    # # 2. แก้ Ngrok ให้ยิงไปหา Nginx (Port 80) แค่รูเดียว
    # ngrok:
    #     image: ngrok/ngrok:latest
    #     container_name: ngrok-tunnel
    #     command:
    #         - "http"
    #         - "nginx:80" # ชี้ไปที่ container nginx พอร์ต 80
    #     environment:
    #         NGROK_AUTHTOKEN: 37cL3fBh4NU3Vr9TdHKsxPhQcr0_6WD9V3QqRMYHL2qhLhGhQ
    #     ports:
    #         - 4040:4040
    #     depends_on:
    #         - nginx
    #     networks:
    #         - marketplace-network

volumes:
    pgdata:
    pgadmin_data:
    ollama_storage:
    uploads_data_dev:
        # Persistent storage for uploaded images (dev environment)


networks:
    marketplace-network:
        driver: bridge
