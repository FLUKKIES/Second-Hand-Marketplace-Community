services:
    postgres:
        image: postgres:15
        container_name: marketplace-db
        env_file:
            - .env
        environment:
            POSTGRES_USER: ${POSTGRES_USER}
            POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
            POSTGRES_DB: ${POSTGRES_DB}
        ports:
            - "${POSTGRES_PORT}:5432"
        volumes:
            - pgdata:/var/lib/postgresql/data
        networks:
            - marketplace-network

    pgadmin:
        image: dpage/pgadmin4
        container_name: marketplace-pgadmin
        env_file:
            - .env
        environment:
            PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
            PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD}
        ports:
            - "${PGADMIN_PORT}:80"
        depends_on:
            - postgres
        volumes:
            - pgadmin_data:/var/lib/pgadmin
        networks:
            - marketplace-network

    # backend:
    #     build: ./backend
    #     container_name: marketplace-backend
    #     env_file:
    #         - ./backend/.env
    #     ports:
    #         - "${PORT}:${PORT}"
    #     volumes:
    #         - ./backend:/app
    #         - /app/node_modules
    #     # environment:
    #         # *** จุดสำคัญ: ประกอบ URL สำหรับใช้ "ภายใน" Docker (ใช้ชื่อ host: postgres) ***
    #         # DATABASE_URL: "postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?schema=public"
    #         # PORT: ${PORT}
    #     depends_on:
    #         - postgres
    #     networks:
    #         - marketplace-network

    # # Frontend
    # frontend:
    #     build: ./frontend
    #     container_name: marketplace-frontend
    #     env_file:
    #         - .env
    #     ports:
    #         - "${FRONTEND_PORT}:3000"
    #     volumes:
    #         - ./frontend:/app
    #         - /app/node_modules
    #     environment:
    #         NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
    #     depends_on:
    #         - backend
    #     networks:
    #         - marketplace-network

volumes:
    pgdata:
    pgadmin_data:


networks:
    marketplace-network:
        driver: bridge
